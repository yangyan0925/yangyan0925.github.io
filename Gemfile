source "https://rubygems.org"

# 使用 github-pages Gem 管理 Jekyll 版本
gem "github-pages", group: :jekyll_plugins

# 如果需要使用特定的插件或 Gems，可以继续在这里添加

# 如果有其他需要的 Jekyll 插件，可以在这里添加
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "hawkins"
  gem "webrick", "~> 1.8"
end

